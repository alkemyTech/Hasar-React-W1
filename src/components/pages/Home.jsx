import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Typography variant="h3" mt={1}>
        HOME
      </Typography>
    </div>
  );
};

export default Home;
